export interface Guide {
  year: string;
  subject: string;
  level: string;
  name: string;
  url: string;
}

export interface ExternalLink {
  name: string;
  url: string;
  category: string;
  description: string;
  color: string;
}